[okta_identity_cloud://<name>]
metric = The metric (data type) you wish to collect
global_account = Select the Okta Account from the global account list(username is Okta domain name, password is API Token)See: http://developer.okta.com/docs/api/getting_started/getting_a_token.html